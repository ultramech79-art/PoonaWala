run = "cd apps/api && uvicorn app.main:app --host 0.0.0.0 --port 8000"

[nix]
channel = "stable-23_11"

[[ports]]
localPort = 8000
externalPort = 80

[env]
VIRTUAL_ENV = "/home/runner/${REPL_SLUG}/venv"
PATH = "${VIRTUAL_ENV}/bin:${PATH}"
PYTHONUNBUFFERED = "1"

# Backend API base URL. Leave empty to use Vite proxy in local dev.
# In production (Cloudflare Pages), set to your Hetzner/API domain:
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=

# Optional: Groq API Key for multimodal failover (starts with gsk_)
# GROQ_API_KEY=
